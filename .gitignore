/vendor
/public/build
/public/hot
/public/storage
/storage/*.key
/storage/logs/*.log
/storage/pail
/docker/data

.env
.env.backup
.env.production
/auth.json

.phpunit.cache
.phpunit.result.cache
.phpstan.cache
*.log

.DS_Store
Thumbs.db
/.idea
/.vscode
/.fleet
/.nova
/.zed
.phpactor.json

# локальные секреты, в git не попадают никогда
.secrets/

# Локальные правки окружения разработчика: сдвинутые порты, если на машине
# уже занят 5434 или 6381 другим проектом. В репозиторий не попадает —
# у проверяющего порты из docker-compose.yml должны работать как есть.
docker-compose.override.yml
